using UnityEngine;

public class CropYield : MonoBehaviour
{
    public int costToSell = 1;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
